[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/canopyd/deploy/Dockerfile.canopyd"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
