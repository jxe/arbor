# CanopyAppKit

App-level models shared by the Canopy app and the packages above the working tree: workspace models and the provider protocol, `WorkspaceCoordinator`, logical URLs, display titles and slugs, `EditorSource` (an editor as a source of local changes; `docs/implementing-editors/editor-source.md`), and `BrowserTabController`. No dependencies.
