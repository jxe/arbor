# CanopyEditor

The Quagmire editor host: `ArborEditorHost` and `ArborEditorSurface`, the document binding over `EditorSource`, and the Markdown codec. It pins an exact Quagmire release; keep `Package.swift` and `project.yml` on the same version and test it only through `swift/scripts/test-canopy-editor-local.sh`.
