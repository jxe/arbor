# ArborSyncClient

`ArborSyncRESTClient` for the daemon's loopback control surface (status, trees, accounts, bootstrap, credential, objects, conflicts, events) and `ArborSyncProcessSupervisor`, which attaches to or launches the control-mode daemon. Twin: `@overstory/arborsync-client`; routes in `docs/arborsync-api.md`.
