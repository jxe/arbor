# OverstoryClient

Transport and account plumbing for a working-tree client: credentials in the platform store, `WorkingTreePlacementService`, `CanopyWatchRunner`, account configuration YAML, and resource consent. It knows nothing about node records. Twin: `@overstory/client`.
