# CanopyAppKit

App-level models shared by the Canopy app and the packages above the working tree: workspace models and the provider protocol, `WorkspaceCoordinator`, logical URLs, display titles and slugs, `DocumentAdmissionMachine` (executing `conformance/client-state-machines.json`), and `BrowserTabController`. No dependencies.
