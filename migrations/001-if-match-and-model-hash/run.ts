// Placeholder: written with Data 009. Must take a data root, refuse to run twice by
// checking meta.schema_version, write new objects before rewriting SQLite in one
// transaction, and print { trees: [{ id, root }] } only.
console.error("not implemented: see README.md");
process.exit(1);
