# Hunch to native Arbor parity matrix

This matrix records user-visible Hunch behavior against Arbor's node-first native app. Hunch is the interaction oracle; ArborKit, provider sessions, ArborSync, and ArborApp remain the owners. Arbor must not inherit Clamshell storage, Hunch identity, or flat-workspace assumptions.

| Capability | Hunch behavior | Arbor behavior | Owner | Status | Automated gate | Manual exact-artifact gate | Deliberate difference |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Native shell | One SwiftUI app on macOS and iOS | One SwiftUI app targeting macOS/iOS 27 | ArborApp | Done | Sequential Xcode app tests | Launch exact macOS/iOS artifacts | Arbor owns its bundle and support paths |
| Document-first layout | Document fills the window; exceptional state overlays it | Quiet single-tab document surface; status and backlinks follow content; transient conflict/save banners | ArborApp + ArborQuagmire | Done | App shell and editor tests | Inspect short/long documents and banners | Arbor retains optional tree chrome |
| Workspace browsing | Home page and search, no permanent tree | Collapsible containing-directory sidebar, parent row, active sibling, breadcrumbs, and context actions | ArborApp + WorkspaceProvider | Done | Containing-directory app tests | Navigate documents, folders, files, collections, placeholders | Required for Arbor's heterogeneous trees; modeled on Arbor browse UI |
| Search | Cmd-P native fuzzy picker over title/body, excerpts, no paths | Sidebar Search opens a native sheet over provider title/body results with excerpts and no host paths | ArborApp + WorkspaceProvider | Done | Provider contract plus app sidebar coverage | Keyboard/touch focus and result activation from the macOS/iOS sidebar | Ranking remains provider-owned rather than Hunch FTS-specific; Arbor does not duplicate Search in page chrome or the menu bar |
| Tabs and windows | Many windows, pushed page frames, independent stacks, shared page coordination | Many windows and optional tabs; each tab exposes its trail as a native `NavigationStack` path while retaining independent presentation state and a shared PageID session | ArborKit + ArborApp | Done; accepted 2026-08-25 | Shared-binding/independent-tabs and pushed-frame path tests | Optional regression: open links/search results as pushed frames; exercise native back, two windows, and multiple tabs | Single-tab strip is hidden to avoid permanent chrome; forward history remains command-only rather than occupying the page toolbar |
| Core block editing | Block types, toggles, marks, autotransforms, nav/edit modes | Shared Quagmire editor with source-preserving Arbor session adapter | Quagmire + ArborQuagmire | Done | Quagmire verification and Arbor codec tests | Keyboard, pointer, touch editing | Persistence is provider admission, not Clamshell writes |
| Block navigation and gestures | Selection/nav mode, indent, reorder, drag, pinch/open, native back | Shared Quagmire behavior plus Arbor navigation host | Quagmire + ArborApp | Done; accepted 2026-08-25 | Quagmire behavior suite plus exact iOS 27 app build | Optional gesture regression on the existing iOS 27 simulator/device | Pinch/open resolves Arbor references, not Hunch URLs |
| Internal links and subpages | One Markdown link type with live target title; moving a subpage is offered from that link's contextual action | Provider-scoped references open through ArborEditorHost; backlinks appear below content; physical Move belongs to an eligible subpage-link action rather than a generic current-page path form | ArborQuagmire + WorkspaceProvider | Done; accepted 2026-08-25 | Codec, backlinks, destination-before-source structural-action, eligible-child relocation, and non-child denial tests | Optional regression: open inline/row links, move an eligible linked child, reject non-child/read-only targets, and use Back | TreeID/PageID scope is retained; arbitrary links never imply hierarchy |
| Title-driven filename rename | After the committed H1 title pauses for two seconds, offer to rename the file to its collision-safe slug; preserve `clamshell-id`, resolve links by ID immediately, display the live title, and lazily heal stale path text as referencing pages synchronize | Observe the admitted document title, offer a non-modal rename once per proposal, flush and perform a provider-authored rename that preserves `PageID`, then update the current navigation reference/sidebar/search/backlinks while PageID-scoped links continue resolving; canonical path hints converge through ordinary provider/sync writes rather than an eager workspace rewrite | ArborApp + ArborQuagmire + WorkspaceProvider + ArborReplica/ArborSync | Implemented; hands-on rename matrix pending | Slug/collision/case/emoji tests; provider contract proving PageID preservation; stable navigation-reference reconciliation; replica/sync ordering coverage | Edit a title on macOS and iOS, accept/dismiss the offer, follow old inbound links from another page/device, relaunch, and confirm filename, Home/current frame, sidebar, search, backlinks, and sync agree | Arbor never writes files directly or copies Clamshell caches; the authority merges the provider mutation and stable TreeID/PageID identity outranks path hints |
| Move-to picker | One grouped searchable picker for headings/toggles and other pages | Cmd-Shift-M opens a grouped searchable picker over legal outline targets and writable documents, including directory documents and tree home documents | ArborApp + ArborQuagmire + WorkspaceProvider | Done | Destination-before-source and destination-enumeration tests | Keyboard/touch grouped picker | Plain directories cannot receive blocks; provider-owned document surfaces can |
| Images and assets | Paste/drag one or many images into `Assets/`, then create ordered image blocks that survive reopen | Quagmire `0.3.0` owns async persistence-before-insertion, ordering, undo, loading/missing presentation, and stale-load cancellation; Arbor stores/reads logical assets through the document's tree-scoped provider and one root `Assets` policy | Quagmire + ArborQuagmire + WorkspaceProvider + ArborClient | Implemented; hands-on image matrix pending | Quagmire 0.3 verification; provider asset/file contracts; Arbor host tests; Hunch macOS tests and exact iOS 27 builds before and after tag | Paste in nav/edit modes, multi-image order, Finder/browser drag, undo/redo, reopen/offline, rename/move, duplicate, read-only/failure, two-window stale-load checks | Quagmire owns host-neutral editor lifecycle; Hunch retains local Clamshell files; Arbor owns tree scope, logical Markdown source, storage, reads, and errors |
| External link presentation | `QuagmireExtras` inline title/favicon metadata with Hunch-owned injected cache | The same injected service renders inline title/favicon metadata from Arbor's application-support cache | QuagmireExtras + ArborQuagmire + ArborApp | Done; accepted 2026-08-25 | Shared cache/TTL/dedup tests plus Arbor host-delegation test | Optional regression: paste URL, replace it quickly, reopen offline, cache expiry | Cache identity/path are Arbor-owned; metadata is never authored or synchronized |
| Voice | `QuagmireExtras` toolbar recording, interrupted recovery, packaged App Intent, and Hunch-owned delivery adapter | `VoiceRecordingSession<PageID>` and `VoiceRecordingButton`; toolbar targets current writable PageID, shortcut targets Home, and all delivery uses provider sessions | QuagmireExtras + ArborApp + WorkspaceProvider | Done; accepted 2026-08-25 | Shared recording/recovery policy tests, active/inactive PageID provider delivery, app metadata tests | Optional device regression for permissions, interruption, retry, and Siri/Action Button | Arbor owns support paths, destinations, phrases, permissions, and error presentation; a literal app-target shortcut provider remains required |
| Transcript polishing | Shared conservative QuagmireExtras block action removes fillers/false starts and repairs punctuation/casing without changing meaning | The same `TranscriptPolishingActions` policy is installed through ArborEditorHost and ordinary admitted writes | QuagmireExtras + ArborQuagmire | Done; accepted 2026-08-25 | Shared policy tests plus Arbor host delegation, stale-result, and provider-admission tests | Optional touch/macOS read-only regression | Arbor owns command placement and durability; transformation policy is shared |
| Recovery | Unified trash/lost/purged block recovery plus automatic salvage | Trash/restore, local document revision recovery as a new local change, sync conflict evidence, and explicit confirmation/error presentation | ArborKit + ArborSync + ArborApp | Implemented; hands-on recovery matrix pending | Provider history/recovery and sync crash-point tests plus recovery presentation build | Restore a page/revision and review conflicts | Authority history stays private; recovery never rewinds authority history and no Clamshell journals are copied |
| Multi-device safety | Per-device logs and iCloud structural merge | Offline replica, immutable roots, authority merge, explicit conflict choice | ArborReplica + ArborSync | Done | Protocol and live two-peer suites | Offline edit, reconnect, conflict presentation | Server-assisted authority sync replaces iCloud co-authoring |
| Feedback and accessibility | Haptics/audio, native focus, actionable banners, VoiceOver labels | App-owned editor feedback, native focus, actionable save/sync/conflict banners, labeled controls, loading/missing-image state, structural-move hints, recovery hints, and an honest status detail sheet | Quagmire + ArborApp | Implemented; VoiceOver/reduced-motion audit pending | App/editor accessibility labels and exact macOS/iOS 27 builds | VoiceOver, reduced motion, and keyboard-only audit | Feedback respects Arbor state labels and Arbor's own preference namespace |
| Menus and shortcuts | Complete navigation/edit/recover/recording command surface | Native undo/redo, page-frame navigation, tab, document/folder creation, workspace/location opening, backlinks, source/properties, save/sync, history, block movement/indent/create, and inline-format commands are focused and capability-gated; macOS page/workspace operations live in the menu bar, Search lives in the sidebar, and neither platform has a catch-all ellipsis menu | ArborApp + Quagmire + QuagmireExtras | Done; accepted 2026-08-25 | App compile, focused editor command contract, and exact App Intent metadata | Optional App Shortcut and iPad keyboard regression | Voice and Account remain direct toolbar actions; generic Copy, current-page Move, and standalone Import Asset are deliberately absent |
| Typography | Hunch app theme uses bundled Inter Variable and established page rhythm | Arbor explicitly owns the same fixed type scale, 708-point measure, indentation, and spacing in an app theme | ArborApp theme | Done | App compile and theme construction | macOS/iOS light/dark visual matrix | Arbor uses the native system face until it owns and bundles a font asset; it never relies on Hunch's process-local registration |
| Collections and ordinary files | Deliberately absent | Honest native read-only/file/collection/placeholder/diagnostic surfaces | ArborApp + WorkspaceProvider | Done | Non-document mutation test | Browse each heterogeneous surface | Arbor intentionally exceeds Hunch's page-only ontology |

## Current UI cluster

Implemented after Plan 017 from live inspection of Hunch `a1e8379` and Arbor's browse UI; voice/content planning was initially reconciled against Hunch `d13087b` and QuagmireExtras `0.2.0`, then carried through exact Quagmire `0.3.0`:

- Hunch-derived: document-first window, hidden single-tab strip, provider Search sheet launched from the sidebar, footer backlinks/status, and transient top banners.
- Arbor-browser-derived: containing-directory sidebar, parent navigation, active sibling treatment, compact breadcrumbs, and contextual row actions.
- Preserved Arbor boundary: every open/mutation still routes through `WorkspaceProvider` and PageID sessions; no physical workspace reads were added.
- Editing/safety pass: grouped outline/document Move To, app-owned document rhythm and feedback, focused native editor commands, inline iOS undo/redo, retryable save failure, and a save/sync status sheet.
- Navigation-frame pass: each tab's browser trail now backs a real SwiftUI `NavigationStack`; document loading shows progress inside the pushed frame and never falls through to raw Markdown while the provider session opens.
- Chrome simplification pass: Search is sidebar-only, iOS opens its modal sidebar from a leading hamburger, macOS contextual page/workspace operations use native menus, and the page-level catch-all ellipsis, generic Copy/Move, and standalone Import Asset surfaces are removed.
- QuagmireExtras pass: Arbor-owned voice destinations/support roots/provider delivery, app-target shortcut phrases, injected external-link previews, and the shared transcript-polishing policy. Joe accepted voice, links, polishing, navigation, and menus on 2026-08-25.
- Quagmire 0.3 pass: async image persistence/loading was proven locally in both consumers, released as exact `0.3.0`, and reverified remotely; Arbor's `/Assets` placement and byte access remain entirely behind `WorkspaceProvider`.
- The rows formerly summarized as “partial parity” were gestures/navigation, internal links/subpages, title rename, images, recovery, and accessibility. They now state their actual implementation and remaining hands-on-audit status individually.
