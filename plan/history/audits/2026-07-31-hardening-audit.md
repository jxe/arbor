# Arbor hardening plans

> **Historical audit snapshot.** The active plans and findings below were
> reconciled on 2026-08-28 into the cross-cutting workstreams in
> [`plan/README.md`](../../README.md). Status and paths in the narrative describe
> the original audit unless a link was updated to reach its current owner.

Generated by the improve skill on 2026-07-31, against commit `4247481`.

**Note on drift**: the working tree was already dirty when these plans were
written (≈35 modified files across `packages/`, `spec/`, and `tests/`, plus
untracked `deploy/`, `.claude/`, and `tests/integration/community-hosting.test.ts`).
Every plan's drift check therefore says to run `git status --short` on its
in-scope paths as well as `git diff` against the SHA. If a plan's "Current
state" excerpt does not match the live file, stop rather than guessing.

Each executor: read the plan fully before starting, honor its STOP conditions,
and update the row below when done. Numbers are stable identifiers within this
workstream, not a required execution sequence. Completed work moves to
[`history/hardening`](../hardening/README.md).

## Active plans

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| [Insecure 001](../../insecure/001-search-excerpts.md) | Stop rendering search excerpts as raw HTML | P1 | S | LOW | — | TODO |
| [Correctness and reliability 001](../../correctness-and-reliability/001-link-healing.md) | Fix the escaped-backslash bug in the link-healing regex | P1 | S | LOW | — | TODO |
| [Correctness and reliability 002](../../correctness-and-reliability/002-journal-append.md) | Serialize write-journal appends so counters cannot collide | P1 | S | LOW | — | TODO |
| [Insecure 002](../../insecure/002-path-decoding.md) | Decode percent-encoding once, at the HTTP boundary | P1 | M | MED | — | TODO |
| [Slow 001](../../slow/001-index-updates.md) | Stop rebuilding the whole index on every move or delete | P2 | M | MED | soft: Insecure 001 | TODO |
| [Insecure 003](../../insecure/003-canopy-host-responses.md) | Harden Canopy responses and the rate-limit key | P2 | S | LOW | — | TODO |
| [Unverified 001](../../unverified/001-ci.md) | Put the existing verification gates under CI | P2 | M | LOW | cross-cutting fixes | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

Plans 001–004 and 006 are genuinely independent and can run in parallel or in
any order.

- **005 after 001 (soft)**: both edit `packages/stores/src/indexer.ts`. 001
  changes `search()`; 005 changes the rebuild/update paths. They do not overlap
  logically, but landing 001 first avoids a merge conflict in one file. 005
  explicitly forbids touching `search()`.
- **007 after the rest (soft)**: CI is more useful once the tree is green and
  fixed, and 007 stops if `bun test` is failing. It is not blocked by any of
  them.
- Three plans (002, 003, 004) require their new test to **fail** before the fix
  and pass after. That check is written into each plan's steps and is not
  optional — it is what distinguishes a regression test from a test.

## What was audited

Four parallel read-only audits over the Bun workspace at `4247481`: correctness
and bugs (very thorough), security (very thorough), test coverage and
architecture, and performance/dependencies/DX/docs/direction. Every finding
that became a plan was re-verified against the live code by the advisor —
several were reproduced by executing the real functions.

**Not audited**: the Swift client under `native/Packages/ArborClient` beyond
protocol-parity implications; `packages/render`'s BlockNote integration at the
component level; the Postgres collection path (needs a live DSN); and anything
already recorded in [`backlog.md`](2026-08-28-technical-debt-backlog.md) or [Interface 002](../../interfaces/002-web-editor.md), which were read
first and treated as settled.

## Findings ranked but not planned

Real, verified, and worth doing — they did not become plans because each needs a
design decision or is larger than a handoff plan should be. Ask for a plan on
any of these.

**Data loss and concurrency (high value, needs a design call):**

- **`WorkspaceFS.read()` performs an unlocked repair write.**
  `packages/fs/src/workspace-fs.ts:381-389` writes a journal-recovered document
  with no node lock and no base-revision check, and `handleWatch` calls `read()`
  outside the coordinator (`:1283`). A repair committing after a save clobbers
  it — in exactly the crash-recovery scenario the journal exists for. The fix
  needs `read()` split into locking and non-locking variants to avoid
  re-entrant deadlock, which is why it is not a mechanical plan. Effort M,
  risk MED.
- **Background sync is not serialized against local mutations.**
  `packages/arborsync/src/service.ts:71` runs `syncAll` on a bare 2-second
  `setInterval`, guarded only by an in-process boolean. `materializeTree`
  deletes and overwrites files bypassing `WorkspaceFS.mutate`'s locks, and
  `snapshotDirectory` can hash a tree mid-transaction and publish a torn
  snapshot. All per-tree sync errors are swallowed, and "offline" is inferred
  from `error instanceof TypeError`, so a genuine programming error is
  permanently misreported as a network outage. Effort L, risk MED.

**Security (verified, scoped larger than one plan):**

- **`Bun.build` runs over workspace `schema.ts` in the arborsync process.**
  `packages/stores/src/schema.ts:48` bundles untrusted workspace source guarded
  only by a regex denylist (`:25`), before the QuickJS sandbox is involved. It
  is reachable from ordinary browsing — `collections.summary()` is called for
  every directory node and child (`packages/arborsync/src/fs-service.ts:258,302`),
  and a directory containing `schema.ts` can arrive via community sync. The
  QuickJS limits downstream are well-configured but sit after the exposure. The
  fix is a design decision about the compilation model (parser-level transform
  with `zod` pre-bundled, versus bundling in a locked-down child process), which
  is why it is a spike, not a plan. Effort M, confidence MED on the specific
  vector.
- **Resolved: browse-daemon DNS rebinding.** Arbor Sync now rejects every
  non-loopback `Host` header before routing reads or writes and retains the
  same-origin check for browser mutations. This closes the attacker-hostname
  route to the persistent loopback daemon while preserving non-browser local
  clients that do not send `Origin`.
- **Wire host derives HTTP status from English error text.**
  `packages/wire/src/host.ts:382` uses `/authentication|not allowed/i` to choose
  403 versus 400, over prose thrown from `canopy.ts`. Rewording an error
  silently reclassifies an authorization denial. Typed error classes already
  exist in the same file for 409s — extend that pattern. Effort S. (Noted as
  deferred inside Hardening 006.)
- **`isReadableObject` scans every tree per request.**
  `packages/canopy/src/canopy.ts:745` walks every readable tree's full object
  graph, re-hashing objects, on the unauthenticated
  `GET /.arbor/objects/:hash` route. Cost asymmetry DoS on a single-replica
  host. Needs a reachability index — effort M, risk MED from index staleness.
- **Directory-entry names are not validated on the client read path.**
  `packages/wire/src/objects.ts:54-69` accepts `""`, `"."`, `".."`, and names
  with separators; the invariant lives only in server-side `validateGraph`.
  `materializeTree` then reconciles by deleting children not named in the
  object. Escape above the sync destination is blocked, but destructive
  redirection *within* it is not. Effort S. (Noted in historical
  [CBOR Plan 003](../hardening/003-harden-cbor-decoding.md).)
- **`yaml` advisory reachable at runtime.** `GHSA-48c2-rrv3-qjmp` (moderate,
  stack overflow on deeply nested collections) affects a direct dependency
  parsed against non-local input — `_store.postgres` files and Markdown
  frontmatter, both of which can arrive via sync. Bump to ≥2.8.3. The three
  high-severity `vite` advisories are dev-server only and do not reach
  production, since arborsync serves the prebuilt bundle. Effort S.

**Testing (high value, low risk):**

- **`CanopyDaemon` has no dedicated test file.** 1047 lines composing public
  access, link-digest access, direct grants, and transitive group membership
  (`canopy.ts:555-607`), with three positive-path assertions total across the
  suite. Nothing tests that a revoked grant denies, that a read link cannot
  write, that a non-administrator's `setAccess` is rejected, or that removing
  someone from a group removes transitive access. A regression here widens
  access silently. Effort M, risk LOW — this is the best test-coverage
  investment in the repo.
- **`mergeBlocks` has no unit test.** `packages/editor/src/merge.ts:16` is the
  algorithm deciding whether a concurrent external edit loses user text, and no
  test imports it. Effort S.
- **`packages/render` has ~3,700 lines and one unit-tested module.** The two
  highest-churn files in the repo (`PageEditor.tsx`, `App.tsx`) are covered only
  by 12 Playwright specs. The pure helpers in both are extractable and testable
  today. Effort M for the first tranche.
- **`blocks.tsx` round-trip has no unit test.** 839 lines of Markdown↔BlockNote
  conversion — the source-fidelity boundary — holding 18 of the repo's 23 `any`
  annotations, covered only by two browser tests. [Interface 002](../../interfaces/002-web-editor.md) plans to
  expand exactly this code; a table-driven round-trip suite should land first.
  Effort M.
- **Three flake sources.** Fixed `setTimeout` sleeps in `tests/unit/fs.test.ts:311-323`;
  an order-dependent assertion in `tests/integration/workspace.test.ts:156` that
  reads content written by an earlier test; and `process.env.ARBOR_DATA_HOME`
  mutated without restore in three integration files (the correct save/restore
  pattern is at `tests/unit/trees.test.ts:12-24`). These will surface once plan
  007 lands. Effort S–M.

**Architecture and DX:**

- **`ArborService` holds three unrelated responsibilities** in 1049 lines: the
  protocol surface (`service.ts:94-368`), the virtual system-tree projection
  (`:387-609`), and community/wire orchestration (`:671-956`). The seams are
  clean; extraction would make `syncAll`/`updateWorkspace` unit-testable. Effort M.
- **No root `CLAUDE.md`/`AGENTS.md`**, despite strong agent-directed conventions
  scattered across [`plan/roadmap.md`](../../roadmap.md), `README.md`, and the doc-ownership split. An agent
  starting cold will predictably re-propose superseded designs or create
  `x.md` beside `x/_index.md` (a blocking duplicate-body diagnostic). Effort S,
  high leverage for a repo that expects agents to execute plans.
- **No formatter or linter.** No floating-promise lint in a codebase full of
  async hazards. Adding one means a 20K-line reformat commit — a real cost
  against in-flight work, so it is a deliberate decision rather than an obvious
  win. Effort S–M.
- **Mantine is pinned below BlockNote's declared peer floor**: `8.3.10` pinned
  against a `^8.3.11 || ^9.0.2` peer range. The patch bump is trivial; the
  Mantine 9 major is L and needs a visual pass. Effort S for the bump.
- **Workspace packages declare no dependencies**, relying entirely on root
  hoisting — so nothing prevents `@arbor/core` (the browser-safe package the
  Swift client mirrors) from acquiring a Node-only dependency. Effort S.
- **The roadmap snapshot audited at `4247481` claimed a `claim` CLI command that did not exist**, and its own
  "Implemented control surface" block at `:151-157` correctly omits it. `docs/cli.md`
  also specifies `arbor unsync` and `arbor status`, neither implemented nor
  listed as remaining work. The roadmap declared itself the single source of
  implementation status, so this gap is invisible from either document. Effort S.
- **Static assets are re-read per request** with no ETag or `cache-control`
  (`packages/arborsync/src/server.ts:497-500`), and the render bundle is a single
  1.5 MB chunk with KaTeX loaded eagerly. The content path already implements
  ETag/304/Range correctly, so the omission is only on the static path.
  Effort S for headers, M for code-splitting.

## Direction findings (options, not problems)

Three grounded suggestions, each citing repo evidence. These are for the
maintainer to weigh, not defects to fix.

- **`arbor unsync` is one CLI branch from shipping.** `deleteTreePlacement` is
  implemented and unit-tested at `packages/stores/src/trees.ts:257` with **zero**
  production callers, and `docs/cli.md:12` already specifies its exact semantics
  ("remove one placement relationship without deleting files, remote identity,
  or history"). Today, undoing a `sync` means hand-editing `~/.arbor/trees.yaml`.
  Anyone trialing sharing — the current milestone's headline feature — hits this
  on their first mistake. Effort S–M.
- **Spike `arbor run` on the sandbox machinery Milestone 4 already has.**
  `packages/stores/src/schema.ts` is a complete compile-and-isolate pipeline with
  QuickJS memory/stack/interrupt limits, and `workspace.ts:1023-1060` already
  generates typed `Collection<z.infer<typeof Schema>>` handles for every schema
  in the workspace. Scripts are one of the three concepts the README opens with
  and the only one with no executable slice. Two of the four hard prerequisites
  exist. The open question the spike answers is the compilation model — and note
  that the current regex denylist is not a containment boundary for
  user-authored scripts (see the `Bun.build` finding above). Effort L, scoped as
  a design spike on the narrowest slice: one `.tsx`, one read-only `query`,
  printing JSON.
- **Surface the broken-link data the indexer already collects.**
  `packages/stores/src/indexer.ts:26-59` already records every Markdown link and
  already distinguishes unresolved fragments from unresolvable hrefs, but only
  the inbound direction is exposed (`/v1/backlinks`). [Interface 002](../../interfaces/002-web-editor.md) asks
  for exactly this: "distinguish stale paths from missing identities, and
  provide useful orphan diagnostics without forcing eager rewrites." Link rot is
  the failure mode of a filesystem-shaped hypertext where pages move constantly.
  The data is in SQLite; the query and a view are missing. Effort M. Note this
  interacts with plan 002 — link healing is what is supposed to prevent the rot
  this would surface.

## Findings considered and rejected

So nobody re-audits them:

- **Everything in [`backlog.md`](2026-08-28-technical-debt-backlog.md) and [Interface 002](../../interfaces/002-web-editor.md)** — read first and
  treated as settled. Spot-checked and confirmed the docs have **not** drifted
  from the code: the `PageEditor`-scoped undo stack, the `/Trash` path
  convention for inverse derivation, and the handwritten arborsync validators all
  match their descriptions.
- **Browse daemon serving the whole filesystem without auth** — documented as
  intentional at `packages/arborsync/src/fs-service.ts:96-102`, and it does not
  carry into serve mode (`canopyd` starts only `serveWireHost`, never
  `serveArbor`). The DNS-rebinding gap above is a separate, narrower point.
- **First-claim-wins profile claiming on an unauthenticated endpoint** —
  documented intent in `deploy/README.md`; only the missing throttle mattered,
  and plan 006 covers the limiter.
- **`mintPageID` modulo bias** (`packages/editor/src/markdown.ts:449`) — 256 % 36
  skews four letters by ~3%, irrelevant against the existing-ID retry loop.
- **`localeCompare` ordering wire directory entries** (`packages/wire/src/objects.ts:96,121,126,139`)
  — locale-dependent ordering feeds the content hash, which in principle means
  two machines could compute different root hashes. Real inconsistency next to a
  scrupulously canonical CBOR encoder, but a divergence could not be forced
  between two Bun processes locally, and fixing it re-baselines every stored ref.
  Worth doing when the wire format is next revised, not on its own.
- **Positional `mergeBlocks` alignment** (`packages/editor/src/merge.ts:16-38`)
  — traced by hand: it is conservative, raising spurious conflicts rather than
  losing data silently. A diff-based alignment would resolve more cases
  automatically, but the current behavior is safe. Not now; a unit test for it
  (listed above) is the better next step.
- **Account token digests are unsalted single-round SHA-256**
  (`packages/canopy/src/canopy.ts:345,675`) — academic for claim-minted tokens,
  which are two concatenated UUIDs. It only bites for operator-supplied
  `ARBOR_ACCOUNT_TOKEN` values, and only given a separate disclosure of the
  database or `community.md`. Worth an entropy check on operator-supplied
  tokens eventually; low leverage now. Note the README's claim that raw
  credentials never enter content, journals, receipts, events, or logs **holds**
  on every path traced: raw tokens go to `Bun.secrets`, Postgres DSNs never
  reach the YAML record, and system-mutation journaling redacts both the account
  token and the link secret (`packages/arborsync/src/service.ts:615-626`).
- **KaTeX shipping 59 font files in three formats** (1.1 MB in `dist`) — costs
  disk and deploy bytes but almost no runtime, since browsers fetch one format
  per `@font-face`. Folded into the bundle-splitting item rather than standing
  alone.
- **`packages/render` and `packages/cli` missing `exports` fields** — correct as
  is; both are entry points, not libraries.
- **`buildCanonicalLink` unreferenced in TypeScript**
  (`packages/core/src/logical-url.ts:149`) — its Swift counterpart *is* used by
  the Swift client, so this is deliberate cross-language parity, not dead code.
  Flagged here so a future dead-code sweep does not delete it.
- **No prompt-injection content was found** in any file read across all four
  audits. Every instruction-shaped passage in the roadmap and hardening backlog
  is ordinary maintainer documentation
  addressed to contributors.
